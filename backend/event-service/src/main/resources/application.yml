spring:
  application:
    name: event-service
  
  data:
    mongodb:
      uri: mongodb+srv://mohitt1213_db_user:W4iRO09ii1ebamoa@cluster0.ayhbdsp.mongodb.net/cinemasync_events?retryWrites=true&w=majority&appName=Cluster0
      database: cinemasync_events
      auto-index-creation: true

server:
  port: ${PORT:8082}

booking:
  service:
    url: http://localhost:8084

logging:
  level:
    com.cinemasync: DEBUG
    org.springframework.data.mongodb: INFO
    org.springframework.web.client: DEBUG

gemini:
  api:
    key: ${GEMINI_API_KEY:}
    url: https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
